# VBN
Vision based navigation for RISE algorithm demo. This repository includes the source code (tracking_mode.py and pose_estimation.py), a Jupyter notebook to run through the different parts of the image processing algorithm and 3 small datasets of images to test the algorithm on. 
