# VBN
Vision based navigation for RISE algorithm demo. This repository includes the source code (tracking_mode.py and pose_estimation.py), a Jupyter notebook to run through the different parts of the image processing algorithm, 3 small datasets of images to test the algorithm on and a results repository which includes the results obtained on the full datasets. 
